# 键盘点动 Demo 代码对比与迁移说明

## 结论

当前机械臂控制程序是 C++，Python 不参与正式控制。当前 Linux 版本保留了 Windows 版本的机器人控制流程，新增 Linux evdev 键盘、Linux SDK 构建、诊断程序和测试。

基线目录：

```text
/home/wy/wydata/keyboard_jog_demo/keyboard_jog_demo_win
```

当前版本：

```text
/home/wy/wydata/keyboard_jog_demo
```

## 与 Windows 版本相比的修改

### 修改 `main.cpp`

文件：`/home/wy/wydata/keyboard_jog_demo/main.cpp`

- 增加 Windows/Linux 条件编译。
- Linux 使用 `SIGINT`、`SIGTERM`、`SIGHUP` 退出处理。
- Linux 增加第 4 个参数：键盘 evdev 设备路径。
- Linux 使用 `EvdevKeyReader` 获取按下和松开事件。
- 原有机器人连接、示教模式、伺服上电、点动和退出清理流程保留。

### 新增 `jog_input.h/cpp`

文件：

```text
/home/wy/wydata/keyboard_jog_demo/jog_input.h
/home/wy/wydata/keyboard_jog_demo/jog_input.cpp
```

定义 `KeySnapshot`、`JogRequest`，并实现方向键到机器人轴号的映射。

| 按键 | 轴号 | 方向 |
|---|---:|---|
| ↑ | 1 | positive=true |
| ↓ | 1 | positive=false |
| ← | 2 | positive=true |
| → | 2 | positive=false |

核心函数：`makeJogRequests()`、`applyEvdevKeyEvent()`。

### 新增 `linux_evdev_key_reader.h/cpp`

文件：

```text
/home/wy/wydata/keyboard_jog_demo/linux_evdev_key_reader.h
/home/wy/wydata/keyboard_jog_demo/linux_evdev_key_reader.cpp
```

使用 Linux `poll()`/`read()` 读取 `/dev/input/...`，处理按下、松开、重复、设备断开和 `SYN_DROPPED`。

### 修改 `CMakeLists.txt`

文件：`/home/wy/wydata/keyboard_jog_demo/CMakeLists.txt`

- SDK 路径改为项目相对路径。
- Linux 链接 `cpp/linux/linux_gcc-9_x64_v2.0.4/libnrc_host.so`。
- 增加 `linux_evdev_key_reader`、`keyboard_input_test` 和 CTest。
- Linux 设置 SDK 动态库 RPATH。
- Windows MSVC/MinGW 和 DLL 复制逻辑保留。

### 新增测试和诊断

```text
/home/wy/wydata/keyboard_jog_demo/keyboard_input_test.cpp
/home/wy/wydata/keyboard_jog_demo/tests/jog_input_tests.cpp
```

诊断程序只检测键盘，不连接机械臂。

## 方向键完整调用链

```text
Linux 键盘设备
  -> EvdevKeyReader::poll()
  -> applyEvdevKeyEvent()
  -> KeySnapshot
  -> makeJogRequests()
  -> sendJog() / stopJog()
  -> robot_start_jogging() / robot_stop_jogging()
  -> libnrc_host.so
  -> TCP 192.168.1.13:6001
  -> 纳博特控制器
```

### 键盘读取

函数：`EvdevKeyReader::poll()`

文件：`/home/wy/wydata/keyboard_jog_demo/linux_evdev_key_reader.cpp`

当前使用设备示例：

```text
/dev/input/by-id/usb-ITE_Tech._Inc._ITE_Device_8910_-event-kbd
```

### 键码转换

函数：`applyEvdevKeyEvent()`

文件：`/home/wy/wydata/keyboard_jog_demo/jog_input.cpp`

`103=Up`、`108=Down`、`105=Left`、`106=Right`、`1=Esc`；`value=1` 是按下，`value=0` 是松开，`value=2` 是自动重复。

### 方向转轴请求

函数：`makeJogRequests()`

文件：`/home/wy/wydata/keyboard_jog_demo/jog_input.cpp`

### 开始点动

函数：`sendJog()`，文件：`/home/wy/wydata/keyboard_jog_demo/main.cpp`

内部调用：

```cpp
robot_start_jogging(socket, request.axis, request.positive);
```

首次按键立即调用，按住后每 100 ms 重发。

### 停止点动

函数：`stopJog()`，文件：`/home/wy/wydata/keyboard_jog_demo/main.cpp`

内部调用：

```cpp
robot_stop_jogging(socket, axis);
```

松键、切换方向、Esc、Ctrl+C、输入设备异常和点动发送失败都会进入停止/清理路径。

### SDK 声明和最终实现

声明文件：

```text
/home/wy/wydata/keyboard_jog_demo/Inexbot_API_v2.0.4-22.07/interface/cpp/interface/nrc_interface.h
```

最终实现文件：

```text
/home/wy/wydata/keyboard_jog_demo/Inexbot_API_v2.0.4-22.07/cpp/linux/linux_gcc-9_x64_v2.0.4/libnrc_host.so
```

`libnrc_host.so` 通过 TCP `6001` 与控制器通信。

## Python 是否参与

文件：`/home/wy/wydata/keyboard_jog_demo/detect_arrow_keys.py`

该文件只调用 Python `curses` 的 `stdscr.getch()`，检测当前终端按键。它不调用 C++、不加载 SDK、不连接控制器、不调用 `robot_start_jogging()`，所以不参与机械臂控制。

## 后续迁移路径

### 输入功能

修改：

```text
linux_evdev_key_reader.*
jog_input.*
```

新增按键或组合键，先进入 `KeySnapshot`，再进入映射函数。

### 机器人功能

修改：`main.cpp`。

新增 Z 轴、关节模式、速度、工具坐标系、暂停/恢复和状态查询，在这里接入 SDK 控制流程。

### SDK 功能

使用：`Inexbot_API_v2.0.4-22.07/interface/cpp/interface/`。

不要修改厂商头文件或 `.so`，新增功能调用 SDK 已提供的接口。

### 构建和测试

修改：`CMakeLists.txt`、`tests/jog_input_tests.cpp`。

每次迁移后执行：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
ctest --test-dir build --output-on-failure
```

## 文件归属

必须保留：

```text
main.cpp
jog_input.h
jog_input.cpp
linux_evdev_key_reader.h
linux_evdev_key_reader.cpp
CMakeLists.txt
Inexbot_API_v2.0.4-22.07/
```

建议保留：

```text
keyboard_input_test.cpp
tests/jog_input_tests.cpp
README.md
使用说明
```

Ubuntu 正式控制不依赖：

```text
detect_arrow_keys.py
key_monitor.cpp
start_key_monitor.cmd
start_robot_demo.cmd
keyboard_jog_demo_win/
```

`keyboard_jog_demo_win/` 作为 Windows 基线保留，不建议继续在其中开发 Linux 功能。

## 当前验证状态

- C++ 程序已连接过 `192.168.1.13:6001`。
- ITE 键盘 evdev 设备已验证方向键按下和松开。
- `robot_start_jogging()` 和 `robot_stop_jogging()` 调用链已接通。
- SDK 动态库可正常加载。
- CTest 当前为 `2/2` 通过。

仍需每台真机确认轴方向、松键停止、断网、急停和安全回路行为。
